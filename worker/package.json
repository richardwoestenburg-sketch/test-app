{
  "name": "daglog-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for Daglog",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
